node_modules
.env
.env.example
.git
.gitignore
npm-debug.log*
README.md

# SQLite files — the container creates its own on every start (see start.sh);
# never ship a local dev database into the image.
prisma/*.db
prisma/*.db-journal
prisma/*.db-wal
prisma/*.db-shm
